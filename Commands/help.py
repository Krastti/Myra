from aiogram import Router
from aiogram.filters import Command
from aiogram.types import Message
import logging

router = Router()
logger = logging.getLogger(__name__)

HELP_MESSAGE = """
<b>🤖 Помощь по боту-секретарю</b>

Вот список доступных команд:

• <b>/start</b> - Показать приветственное сообщение
• <b>/help</b> - Показать это сообщение помощи

<b>Управление задачами:</b>
• <b>/add_task &lt;описание&gt;</b> - Добавить новую задачу (скоро)
• <b>/list_tasks</b> - Просмотреть список задач (скоро)
• <b>/complete_task &lt;номер&gt;</b> - Отметить задачу как выполненную (скоро)

<b>Напоминания:</b>
• <b>/set_reminder &lt;время&gt; &lt;сообщение&gt;</b> - Установить напоминание (скоро)

<b>Другое:</b>
• <b>/ask &lt;ваш вопрос&gt;</b> - Задать вопрос ИИ-ассистенту (скоро)

Просто отправьте нужную команду, и я постараюсь помочь!
"""

@router.message(Command('help'))
async def command_help_handler(message: Message) -> None:
    logger.info(f"Пользователь {message.from_user.full_name} (ID: {message.from_user.id}) использовал команду /help.")
    try:
        await message.answer(HELP_MESSAGE)
    except Exception as e:
        logger.error(f"Ошибка при обработке команды /help: {e}", exc_info=True)

# Не забываем экспортировать роутер
__all__ = ["router"]