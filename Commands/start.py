from aiogram import Router
from aiogram.filters import CommandStart
from aiogram.types import Message
import logging

router = Router()
logger = logging.getLogger(__name__)

HELLO_MESSAGE = '''
<b>Привет! 👋 Это ваш личный бот-секретарь.</b>

Я помогу вам управлять задачами, оставаться в курсе важных событий и быть более организованным. Вот что я уже умею:

• <b>/start</b> - Показать это приветственное сообщение
• <b>/help</b> - Получить список доступных команд (скоро)
• <b>Управление задачами</b> - Добавлять, просматривать, отмечать выполненным (скоро)
• <b>Напоминания</b> - Устанавливать напоминания о важных событиях (скоро)
• <b>Информационный ассистент</b> - Отвечать на вопросы с помощью ИИ (скоро)

Просто отправьте мне команду или сообщение, и я постараюсь помочь!
'''

@router.message(CommandStart())
async def start(message: Message):
    logger.info(f'Пользователь {message.from_user.full_name} использовал команду /start.')
    try:
        await message.answer(HELLO_MESSAGE)
    except Exception as e:
        logger.error(f'Ошибка при обработке команды /start: {e}', exc_info=True)

__all__ = ['router']